# Better Auth
BETTER_AUTH_SECRET=replace-with-a-32-plus-character-random-secret
BETTER_AUTH_URL=http://localhost:3005

# Frontend origin allowed to call the auth API
FRONTEND_URL=http://localhost:5173

# SQLite database
DATABASE_URL=./data/primebiller.sqlite

# Demo account
DEMO_EMAIL=manikantakambala12@gmail.com
DEMO_PASSWORD=

# Email delivery for password-reset OTPs
# Use a real SMTP account in development/production.
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM="Girder <no-reply@example.com>"
